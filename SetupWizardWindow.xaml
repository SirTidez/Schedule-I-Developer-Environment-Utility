<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Schedule_I_Developer_Environment_Utility.SetupWizardWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Initial Setup Wizard" FontSize="20" FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Spacing="12">
            <TextBlock x:Name="StepTitle" Text="Step 1 of 4: Select Library &amp; Game" FontWeight="SemiBold" />

            <!-- Step 1: Libraries + Game detection -->
            <StackPanel x:Name="Step1Panel" Spacing="8">
                <TextBlock Text="Steam Libraries:" />
                <ListView x:Name="LibrariesList" ItemsSource="{Binding Libraries}" Height="120" SelectionMode="Single" SelectedItem="{Binding SelectedLibrary, Mode=TwoWay}"/>

                <TextBlock Text="Game Install Path:" />
                <TextBox Text="{Binding GameInstallPath, Mode=TwoWay}" />

                <TextBlock Text="Games in selected library:" />
                <ListView x:Name="GamesList" ItemsSource="{Binding GameFolders}" Height="160" SelectionMode="Single" SelectionChanged="OnGameFolderSelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Spacing="4" x:Name="DetectedBranchPanel" Visibility="Collapsed">
                    <TextBlock Text="Detected Steam Branch:" />
                    <TextBlock Text="{Binding DetectedBranchDisplay}" />
                </StackPanel>

                <muxc:InfoBar
                    Severity="Error"
                    IsOpen="{Binding Step1ErrorOpen}"
                    Message="{Binding Step1ErrorMessage}"
                    IsClosable="True"/>
            </StackPanel>

            <!-- Step 2: Managed environment path -->
            <StackPanel x:Name="Step2Panel" Spacing="8" Visibility="Collapsed">
                <TextBlock Text="Managed Environment Path:" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBox Text="{Binding ManagedEnvironmentPath, Mode=TwoWay}" Width="420" />
                    <Button Content="Browse..." Click="OnBrowseManagedPath" Width="100" />
                </StackPanel>

                <TextBlock Text="Suggestions:" />
                <ListView x:Name="ManagedSuggestionsList" ItemsSource="{Binding ManagedPathSuggestions}" Height="120" SelectionMode="Single" SelectionChanged="OnManagedSuggestionSelectionChanged" />

                <muxc:InfoBar
                    Severity="Error"
                    IsOpen="{Binding Step2ErrorOpen}"
                    Message="{Binding Step2ErrorMessage}"
                    IsClosable="True"/>
            </StackPanel>

            <!-- Step 3: Branch selection -->
            <StackPanel x:Name="Step3Panel" Spacing="8" Visibility="Collapsed">
                <TextBlock Text="Select Branches:" />
                <ItemsControl ItemsSource="{Binding BranchChoices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- Step 4: Branch switch/validation -->
            <StackPanel x:Name="Step4Panel" Spacing="8" Visibility="Collapsed">
                <TextBlock Text="Switch to target Steam branch:" FontWeight="SemiBold" />
                <TextBlock Text="{Binding CurrentTargetBranchDisplay}" />
                <TextBlock Text="Open Steam and switch the game's branch, then click Recheck." />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="Recheck" Click="OnRecheckBranch" Width="100" />
                    <muxc:InfoBar Severity="Error" IsOpen="{Binding BranchSwitchErrorOpen}" Message="{Binding BranchSwitchErrorMessage}" IsClosable="True"/>
                </StackPanel>
            </StackPanel>

            <!-- Step 5: Copy files -->
            <StackPanel x:Name="Step5Panel" Spacing="8" Visibility="Collapsed">
                <TextBlock Text="Copying game files to managed environment" FontWeight="SemiBold" />
                <TextBlock Text="{Binding CopyStatus}" />
                <ProgressBar Minimum="0" Maximum="100" Value="{Binding CopyProgress}" IsIndeterminate="{Binding CopyIndeterminate}" />
                <TextBlock Text="Copy Output:" FontWeight="SemiBold"/>
                <ListView x:Name="CopyLogList" ItemsSource="{Binding CopyLogs}" Height="180" />
                <muxc:InfoBar Severity="Error" IsOpen="{Binding CopyErrorOpen}" Message="{Binding CopyErrorMessage}" IsClosable="True"/>
            </StackPanel>

            <!-- Step 6: Summary -->
            <StackPanel x:Name="Step6Panel" Spacing="6" Visibility="Collapsed">
                <TextBlock Text="Summary" FontWeight="SemiBold" />
                <TextBlock Text="Review your selections before saving." />
                <TextBlock Text="Steam Path:" />
                <TextBlock Text="{Binding SteamPath}" />
                <TextBlock Text="Game Path:" />
                <TextBlock Text="{Binding GameInstallPath}" />
                <TextBlock Text="Managed Env Path:" />
                <TextBlock Text="{Binding ManagedEnvironmentPath}" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button x:Name="BackButton" Content="Back" Click="OnBack" Width="80"/>
            <Button x:Name="NextButton" Content="Next" Click="OnNext" Width="80"/>
            <Button x:Name="FinishButton" Content="Finish" Click="OnFinish" Width="90" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>
